/**
 * clubAI.js
 *
 * Placeholder / extension point.
 *
 * The original Apex Football Simulator app.js does not contain a separate
 * transfer market, club-AI negotiation, or scouting subsystem — clubs, squads
 * and competitions are fixed for the season/tournament being played. This
 * file is kept empty (rather than omitted) so the requested folder layout
 * exists ready to build into, e.g. a future transfer window feature.
 *
 * If you add code here, also add an entry for it to manifest.json (see
 * build.js) so it gets picked up by the build.
 */
