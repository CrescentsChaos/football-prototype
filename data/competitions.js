/**
 * competitions.js
 *
 * Placeholder / pointer file.
 *
 * Competition/league membership data (which clubs belong to which domestic
 * league) is embedded as `leaguesData` in js/state.js, and the logic that
 * builds fixtures/tables/knockout brackets for those competitions lives in
 * simulation/seasonEngine.js and simulation/tournamentEngine.js. This file
 * is a stable place to move that data to on its own if it grows — for now
 * it's left as a marker so the folder layout matches the requested structure.
 */
